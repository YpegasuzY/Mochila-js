const texto = 'aqui temos uma string normal';

const texto2 = "já esta, apesar das aspas dulas ao invés das simples, também é uma string";

const numeroRegistro = 31472;
const stringTemplate = `O número que quero registrar é o ${numeroRegistro}`;


console.log(stringTemplate);